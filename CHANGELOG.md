# hindsight - Changelog

**Last Updated:** 2026-02-20
**Generated by:** Squeegee

All notable changes to this project are documented here. Entries are auto-generated from git commits.

---

<!-- SQUEEGEE:AUTO:START changelog-entries -->
### 2026-02-18

**Documentation:**
- update CLAUDE.md (`4be2fe4`)

### 2026-02-09

**Other:**
- Add Supabase tenant extension as built-in (#267) (`e99ee0f`)

**Fixed:**
- do not log db user/password (#312) (`c568094`)
- **openclaw:** prevent memory wipe on every session (#323) (`981cf60`)

**Added:**
- add docker-compose example (#313) (`5179d5f`)

### 2026-02-08

**Added:**
- improve mcp tools based on endpoint (#318) (`d90588b`)

### 2026-02-07

**Other:**
- improve Node.js client example (#320) (`d0f67c9`)

### 2026-02-06

**Added:**
- add TenantExtension auth to MCP endpoint (#286) (`fedfb49`)
- slim docker distro (#314) (`f648178`)
- support markdown in reflect and mental models (#307) (`c4ef090`)

**Fixed:**
- hindsight-embed profiles are not loaded correctly (#316) (`0430588`)
- **ci:** resolve flaky test failures in api tests (#311) (`fa4cbf7`)
- **openclaw:** remove format:uri to fix ajv warning (#309) (`96f4872`)

**Other:**
- update claude-code usage terms (#315) (`2af0e08`)

**Maintenance:**
- ensure python 3.14 compatibility (#310) (`2109397`)

### 2026-02-05

**Maintenance:**
- ensure backwards/forward compatibility of the API (#306) (`0d8d805`)

**Other:**
- 0.4.9 changelog (`1cd8362`)
- fixed cast error (#300) (`093ecff`)

**Documentation:**
- add AI SDK integration documentation (#304) (`90ad003`)

**Fixed:**
- tagged directives should be applied to tagged mental models (#303) (`278718d`)

### 2026-02-04

**Other:**
- Release v0.4.9 (`85b9074`)
- Fix recall endpoint timeout handling and add query length validation (#298) (`dd621a6`)

**Added:**
- ai sdk integration (#299) (`7e339e1`)
- improve mental models ux on control plane (#297) (`7097716`)
- HindsightEmbedded python SDK (#293) (`d3302c9`)
- **hindsight-litellm:** support streaming on wrappers (#296) (`665877b`)
- **openclaw:** add dynamic per-channel memory banks (#290) (`9a776e9`)
- **openclaw:** add external Hindsight API support (#289) (`6b34692`)

**Fixed:**
- improve claude code and codex for /reflect (#285) (`a43d208`)
- hide hf logging (#295) (`34d9188`)
- **openclaw:** improve shell argument escaping (#288) (`63e2964`)

**Documentation:**
- expand external API configuration section for OpenClaw (#294) (`d02affd`)

### 2026-02-03

**Other:**
- update cookbook (#284) (`d5403a4`)
- changelog for 0.4.8 (#283) (`a24941f`)
- Release v0.4.8 (`21b25fe`)

**Fixed:**
- improve embed ux with rich logging and profile isolation (#282) (`794a743`)
- **sec:** upgrade vulnerable deps (#254) (`038a9c2`)

**Added:**
- improve openclaw and hindisght-embed params (#279) (`749478d`)

### 2026-02-02

**Other:**
- load operation validator extension in worker process (#280) (`96f0e54`)
- Propagate request context through async task payloads (#273) (`44d9125`)

**Fixed:**
- custom pg schema is not reliable (#278) (`3825506`)

**Added:**
- **embed:** add hindisght-embed profiles (#277) (`6c7f057`)
- support for codex and claude-code as llm (#276) (`539190b`)
- print version during startup (#275) (`1499ce5`)
- **openclaw:** add llmProvider/llmModel plugin config options (#274) (`8564135`)

### 2026-02-01

**Other:**
- Add MentalModelRefreshContext and pre-operation validation for mental model create/refresh (#271) (`35127d5`)

### 2026-01-31

**Other:**
- Release v0.4.7 (`86c733c`)
- fix release script (`cb7ebe8`)
- Revert "fix release script" (`6155090`)
- fix release script (`af6bd1b`)
- fix release script (`579b10b`)
- Add extension hooks for mental model operations (#260) (`9c3fda7`)

**Added:**
- **hindsight-embed:** external API support + OpenClaw fixes (#263, #264) (#265) (`4b57b82`)
- **docker:** preload tiktoken encoding during build (#249) (`039944c`)

**Fixed:**
- **hindsight-embed:** respect HINDSIGHT_API_DATABASE_URL if already set (#262) (`f0cb192`)
- sanitize null bytes from text fields before PostgreSQL insertion (#238) (`ef9d3a1`)
- worker doesn't pick up correct default schema (#259) (`d788a55`)

### 2026-01-30

**Other:**
- Release v0.4.6 (`c8ae82d`)
- show embed page (#255) (`1530c09`)
- Release v0.4.5 (`fe88bdf`)
- Release v0.4.4 (`c33b9b8`)
- sync docs (`5dc4519`)
- sync docs to 0.4 (`1ba70f8`)
- openclawd (`fe15b5e`)
- changelog (`10e21f7`)
- Release v0.4.3 (`7d3ac5d`)
- Switch Vertex AI provider to native genai SDK (#242) (`49ae55a`)

**Fixed:**
- openclaw improve config setup (#258) (`27498f9`)
- openclaw binds embed versioning (#256) (`1163b1f`)
- retain async with timestamp might fails (#253) (`cbb8fc6`)
- rename openclawd to openclaw (#252) (`b364bc3`)
- retain async fails if timestamp is set (#251) (`35f0984`)
- deadlock in worker polling (#250) (`f4f86e3`)
- rename moltbot to openclawd (#246) (`728ce13`)
- **docker:** add retry logic for ML model downloads (#248) (`ecc590c`)
- improve doc on vertexai and mcp (#247) (`381c96c`)
- **auth:** skip tenant auth for all internal background tasks (#240) (`d57e863`)
- **control-plane:** pass API key to dataplane for tenant auth (#243) (`03bf13e`)
- **control-plane:** handle undefined response.data in graph route (#239) (`751f99a`)

**Documentation:**
- improve openclawd integration docs - align with blog narrative (`ff47814`)

**Maintenance:**
- remove dead code (#245) (`ab5e31f`)

**Added:**
- **mcp:** add Bearer token authentication and tenant auth propagation (#241) (`0da77ce`)
- **cli:** add --wait flag for consolidate and --date filter for document list (#244) (`ff20bf9`)

### 2026-01-29

**Added:**
- support vertex as llm provider (#233) (`c2ac7d0`)
- add more config options for llm retries (#234) (`3f211f0`)
- add real-time timing breakdown logging for consolidation (#235) (`8781c9f`)
- moltbot integration (#216) (`12e9a3d`)
- consolidation performance benchmark and optimization (#227) (`b43ef98`)

**Fixed:**
- run migrations on tenant schemas at startup and harden worker poller (#237) (`657fe02`)
- pass tenant extension to worker MemoryEngine for correct schema context (#236) (`9c95a1a`)
- hindsight-embed on macos crashes (#228) (`c16ccc2`)
- **doc:** improve docs versioning and release (#231) (`a7c094d`)
- /version endpoint return wrong version (#224) (`cfcc23c`)

**Other:**
- Release v0.4.2 (`1554007`)
- Release v0.4.1 (`b8f06a0`)
- hide next version (#226) (`f17703f`)

### 2026-01-28

**Other:**
- README video (`7300d5b`)
- README tweak (`81c82d9`)
- Updated video in readme (`7551e65`)
- improve readme (#223) (`d2b797f`)
- release notes for 0.4.0 (#217) (`20f2b92`)
- add blog (#201) (`1bf9035`)
- Release v0.4.0 (`2118d0a`)

**Fixed:**
- search_mental_models uuid type mismatch after text id migration (#225) (`94cc0a1`)
- add defensive error handling to PyTorch device detection (#221) (`67c4788`)
- include correct __version__ in python packages (#218) (`fccbdfe`)
- **embed:** daemon process XPC connection crash on macos (#215) (`e5fc6ee`)
- graph endpoint not showing links for observations (#214) (`bb0e031`)

**Added:**
- support different default pg schema (#222) (`2b72e1f`)
- add custom extraction prompt (#213) (`3172e99`)

**Maintenance:**
- cleanup benchmarks runner with old flags (#212) (`1c9a7a0`)

### 2026-01-27

**Fixed:**
- misc fixes for observations and mental models (#209) (`90e370e`)
- multi-tenant schema context for worker task execution (#208) (`83f44c4`)
- include tags, created_at, proof_count in graph table_rows (#207) (`7bdb8fc`)

**Maintenance:**
- drop dead code (#210) (`084242a`)
- internal renames (#204) (`5b52a84`)

### 2026-01-26

**Added:**
- **litellm:** support tags and mission in litellm package (#202) (`f3c5a9c`)
- **litellm:** async retain, reflect support, and API cleanup (#167) (`1d4879a`)

**Fixed:**
- **ui:** reflections based on don't show up all contents (#203) (`5832b90`)
- improve mental model consolidation (#197) (`8e39cb7`)

**Maintenance:**
- add upgrade tests (#200) (`50fa2ed`)
- versioned docs (#198) (`31b5c58`)

**Other:**
- mental models (#199) (`522b71a`)
- Pass api_key to Hindsight client in litellm integration (#193) (`c0ca9b0`)

### 2026-01-23

**Added:**
- **mcp:** add timestamp to retain (#190) (`b378f68`)
- support for npx add-skill (#191) (`ec22317`)
- **python-sdk:** add tags filtering support to high-level client (#186) (`aebef94`)

**Other:**
- fix skill names (`9c2df9d`)
- Fix Gemini tool response format by including function name (#187) (`66abad6`)

### 2026-01-22

**Added:**
- revisit mental models, directives and reflections (#179) (`9db64ec`)

**Fixed:**
- simplify pytorch model initialization to prevent meta tensor issues (#185) (`ddaa5f5`)
- sometimes memories gets extracted in the wrong language (#184) (`87d4a36`)

### 2026-01-20

**Fixed:**
- improve pytorch model initialization to prevent meta tensor issues (#180) (`0bf85a3`)

**Maintenance:**
- drop unused access_count column (#178) (`16b85a4`)

**Added:**
- new 'worker' service (#176) (`4c79240`)

### 2026-01-19

**Fixed:**
- pytorch init failures (#175) (`0284595`)
- **sec:** upgrade vulnerable deps (#174) (`bac4b24`)

**Added:**
- **clients:** mental models api (#172) (`fe4ed1d`)
- improve mental model refresh and add directives (#166) (`63a65d0`)

**Maintenance:**
- unify agents.md and claude.md (#173) (`3290f4b`)

**Other:**
- Add structured JSON logging support (#170) (`870cfcc`)

### 2026-01-16

**Other:**
- refinement for 0.3.0 new features (#159) (`4476a10`)

**Added:**
- introduce mental models (#132) (`4f28338`)

### 2026-01-15

**Added:**
- **cli:** accept more file types on retain-files (#163) (`1eeced3`)

### 2026-01-14

**Other:**
- Fix skill installer test examples to use meaningful content (#160) (`55c216e`)

**Added:**
- add cloud mode to skill installer for team memory sharing (#158) (`e64d363`)

### 2026-01-13

**Other:**
- update expired Slack invite link (#157) (`70ce979`)
- changelog for 0.3.0 (#156) (`de13250`)
- Release v0.3.0 (`a75dcfe`)
- Load extensions in server.py for multi-worker deployments (#155) (`f5f3fca`)

**Added:**
- add memory tags (#152) (`20c8f8b`)
- support litellm gateway (#154) (`d47c8a2`)
- add tenant to metrics labels (#151) (`1ffc2a4`)
- support custom url for openai embeddings & cohere (#150) (`fa53917`)

**Fixed:**
- batch queries on recall (#149) (`5991308`)

### 2026-01-12

**Fixed:**
- improve mpfp retrieval (#146) (`7935b0a`)
- entities list only show 100 entities (#142) (`26bf571`)

### 2026-01-09

**Fixed:**
- improve graph retrieval on large memory banks (#141) (`6232e69`)
- **typescript-client:** Add error handling to all API methods (#139) (`0d0abaa`)
- improve tei client parameters (#137) (`a6798f7`)

**Maintenance:**
- frozen uv sync (#138) (`4135a6c`)

**Other:**
- performance improvements (#140) (`eb2702b`)

**Added:**
- retain modes (#136) (`fb31a35`)

### 2026-01-08

**Fixed:**
- misc perf improvements (#133) (`ba99b44`)
- duplicated causal relationships and token optimization (#126) (`49e233c`)
- **mcp:** add back bank list and create_bank tools (#123) (`9fd5679`)
- ui shows only 1000 memories (#121) (`67c1a42`)

**Other:**
- Fix embedding dimension for tenant schemas (#135) (`6fe9314`)
- Fix stats endpoint missing tenant authentication (#134) (`d6ff191`)

**Maintenance:**
- fix flak tests (#131) (`3bb6a38`)
- add flag to not include ml libs in docker image (#130) (`b5df865`)

**Added:**
- add operation_id to retain response (#129) (`1dacd0e`)
- delete memory bank (#127) (`4b82d2d`)
- add configs for database connection (#128) (`33fac2c`)
- support different provider/models per operation (#125) (`e6709d5`)
- support for multilingual content (#124) (`c65c6a9`)
- support cohere as embeddings and reranker (#122) (`4de0730`)
- add metrics for llm call latency (#120) (`5e1f13e`)
- **mcp:** add async_processing parameter to retain tool (#95) (`37fc7fb`)
- Add per-request LLM token usage metrics (#117) (`29a542d`)
- **helm:** add existingSecret support (#119) (`ecc1f31`)

### 2026-01-07

**Added:**
- run db migrations offline (optionally) (#114) (`233bd2e`)
- backup/restore (#110) (`67b273d`)

**Fixed:**
- **security:** fix qs - CVE-2025-15284 (#113) (`b3becb6`)
- improve causal links detection (#111) (`2a00df0`)
- make retain max completion tokens configurable (#109) (`7715a51`)

**Maintenance:**
- pin rust lock version (#112) (`5a3090b`)

**Other:**
- Load .env file automatically on startup (#104) (`c06d9b4`)
- Fix Python SDK not sending Authorization header (#106) (`39e3f7c`)

### 2026-01-05

**Fixed:**
- groq llm with free tier doesn't work (#102) (`d899d18`)

**Added:**
- configurable embedding dimensions + OpenAI Embeddings (#101) (`70de23e`)
- **mcp:** Add multi-bank access and new MCP tools (#82) (`6b5f593`)
- Add user-provided entities support to retain endpoint (#91) (`dd59bc8`)
- Add local LLM improvements for reasoning models and Docker startup (#88) (`eea0f27`)

**Other:**
- Release v0.2.1 (`1984936`)
- changelog for 0.2.0 (and regenerate clients) (#99) (`4f21886`)
- Release v0.2.0 (`5e65691`)
- add mcp integration tests and increase test coverage (#98) (`76fd052`)

**Maintenance:**
- add pre-commit setup instructions (`964537f`)

### 2026-01-02

**Other:**
- Feature/graph viz (#85) (`1a62069`)

**Fixed:**
- **mcp:** Chain MCP lifespan with FastAPI app lifespan (#81) (`6b78f7d`)

**Added:**
- **config:** Add configurable observation thresholds (#83) (`54e2df0`)

### 2026-01-01

**Other:**
- Add operation validator extension support with proper HTTP error handling (#86) (`ce45d30`)

**Added:**
- add max_tokens and structured output to /reflect (#74) (`d49e820`)
- **doc:** add new config options and supported providers (#84) (`c8c7603`)
- Add Anthropic Claude and LM Studio provider support (#36) (`787ed60`)

### 2025-12-24

**Other:**
- Add model providers on README (`967e586`)

### 2025-12-23

**Other:**
- Load operation validator extension in main entry point (#72) (`dfa7cec`)
- add skills documentation (#73) (`36e48a7`)
- Release v0.1.16 (`786b1ec`)
- 0.1.15 changelog (`c9f3657`)
- Release v0.1.15 (`0ae0374`)
- Release v0.1.14 (`e599346`)

**Fixed:**
- hindsight-embed release version (`f14f277`)
- embed get-skill installer (#69) (`0b352d1`)

**Added:**
- delete document from ui (#71) (`f7ff32d`)
- **misc:** update clients types, test coverage,  improve /health endpoint and add changelog (#70) (`e06a612`)

### 2025-12-22

**Other:**
- Release v0.1.13 (`c882511`)
- Release v0.1.12 (`6168a77`)
- add documentation for extensions (#62) (`26850a0`)
- Improve LLM JSON parsing error handling with retry logic and detailed logging (#61) (`a831a7b`)

**Fixed:**
- **ui:** timestamp is not considered in retain (#68) (`234d426`)
- propagate exceptions from task handlers to enable retry logic (#65) (`904ea4d`)
- ollama structured support (#63) (`32bca12`)

**Added:**
- refactor hindsight-embed architecture (#66) (`e6511e7`)
- add hindsight-embed and native agentic skill (#64) (`da44a5e`)
- extensions (#54) (`2a0c490`)

### 2025-12-19

**Maintenance:**
- finalize test for the documentation code (#57) (`d405b4f`)

**Fixed:**
- set max_completion_tokens to 100 in llm validation (#59) (`b94b5cf`)

### 2025-12-18

**Other:**
- add openai api compatible note (`6d820ef`)
- changelog for 0.1.11 (`cf8882a`)
- Release v0.1.11 (`490fccd`)
- update changelog for 0.1.10 (`9053a51`)
- Release v0.1.10 (`f2c28cf`)
- fix release process (`9474f95`)
- Release v0.1.9 (`6a0c034`)
- Add documentation code validation system (#43) (`8ecb5d3`)

**Fixed:**
- docker image and control plane standalone build (`2948cb6`)
- docker image build and startup (#46) (`b52eb90`)

**Maintenance:**
- make release faster and restartable (`67fc532`)

**Added:**
- simplify mcp installation + ui standalone (#41) (`1c6acc3`)

### 2025-12-17

**Fixed:**
- add procps to Docker image and smoke test to release workflow (#45) (`ae80876`)
- bank list response with no name banks (`04f01ab`)
- retain async fails (#40) (`63f5138`)
- bank selector race condition when switching banks (#38) (#39) (`e468a4e`)

**Other:**
- Add Hindsight Cloud links to README and docs (#42) (`476a62d`)
- Release v0.1.8 (`5aaa769`)
- Update README.md (`c0a0f44`)

### 2025-12-16

**Other:**
- add run benchmarks instructions (`84927cc`)
- README updates (`a6e8944`)
- Release v0.1.7 (`f6d890f`)
- 0.1.6 changelog (`656777c`)
- Release v0.1.6 (`b36807a`)
- less verbose git hooks (`11ac9cd`)
- bump pg0 0.11.x and improve documentation (#33) (`47be07f`)

**Maintenance:**
- check compatibility with python 3.11, 3.12 and 3.13 (#35) (`1fa8d91`)

**Fixed:**
- doc build and lint files (#34) (`9394cf9`)

**Added:**
- support for gemini-3-pro and gpt-5.2 (#30) (`bb1f9cb`)
- add local mcp server (#32) (`7dd6853`)

### 2025-12-15

**Other:**
- enable model tests on ci (#29) (`1cef364`)
- fix doc link styling (`dff293c`)
- changelog generator (`f4bc844`)
- models doc (`ae26a86`)
- Release v0.1.5 (`183b9da`)
- litellm release integration (`8a7c6e4`)
- Added hindsight_liteLLM implementation (#17) (`dfccbf2`)

### 2025-12-14

**Other:**
- Trademark to README (`dfea4db`)

### 2025-12-12

**Other:**
- Change npm packaging structure and fix contributing info (#16) (`fcea8af`)
- switch to pg0-embedded (#28) (`94c2b85`)
- fix 400 retries on llm (`70983f5`)
- README banner (`44e9571`)
- fix recall trace visualization (`922164e`)

**Fixed:**
- add DOM.Iterable lib to resolve URLSearchParams.entries() type error (#27) (`160c558`)
- upgrade Next.js to 16.0.10 to patch CVE-2025-55184 and CVE-2025-55183 (#25) (`f018cc5`)

**Added:**
- add optional graph retriever MPFP (#26) (`7445cef`)

### 2025-12-11

**Other:**
- Fix base CI issues and the defaults in .env.example (#24) (`d6b7b9b`)
- fix cli installer (`158a6aa`)
- fix cli installer (`38e73a1`)
- Update Docker run command in README o3 mini (`2c1be4c`)
- Release v0.1.4 (`f148d3e`)
- fix docs on clients (`99db7b2`)
- fix docs build (`ebc85a5`)
- Release v0.1.3 (`ae30882`)
- brandind and misc fixes (`fa554b8`)

### 2025-12-10

**Other:**
- README banner (`f813a80`)
- Fix README images (`f7e8b10`)
- Release v0.1.2 (`522a491`)
- fix docker image (`1056a20`)
- Release v0.1.1 (`01ba974`)
- helm chart updates v1 (`44e79fe`)
- improve docs (`94665b2`)
- improve llms.txt (`52826de`)
- add llms.txt (`0000c54`)
- add llms.txt (`e677a01`)
- add llms.txt (`4191597`)
- add tei support (`e722a48`)
- fix openapi tags (`f7789f4`)

**Fixed:**
- make sure openai provider works + docs updates (#23) (`f42476b`)

### 2025-12-09

**Other:**
- Release v0.1.0 (`edbf887`)
- improve ci and tests (#22) (`040eb33`)
- Update README (`cffb14f`)
- update doc (`3ebe262`)
- update openapi spec (`04b2fcf`)
- Add RAG vs Hindsight examples (#15) (`bbfdcd3`)
- rm results dir (`e96cb96`)

### 2025-12-08

**Other:**
- Draft of new readme (`3e8426d`)
- improve retain performances, caching and tests (`eef43f5`)
- fix entity and migrate memory disposition (`76cfa8f`)
- Increase graph neighbor limit and benchmark improvements (#18) (`3bb0a58`)

**Fixed:**
- upgrade Next.js to 16.0.7 to patch CVE-2025-66478 (#19) (`b0c7bba`)
- update Docusaurus config for custom domain (#20) (`b5abeb5`)

**Documentation:**
- update documentation URL to custom domain (#21) (`6daa3ad`)

### 2025-12-05

**Other:**
- fix readme (`cf2f739`)
- Release v0.0.21 (`2b7b26c`)
- fix node build (`c414900`)
- Release v0.0.20 (`e2fea8f`)
- fix py client (`841fe65`)
- Release v0.0.19 (`83dab57`)
- gemini support (`5ad2dfe`)

### 2025-12-04

**Other:**
- fix migration (`ebe468e`)
- fix migration (`f69ea6e`)
- fix migration (`ab10162`)
- fix migration (`1053e9f`)
- fix delete with pooler (`f7884f5`)
- new names (`06b956a`)
- fix db migration (`718b702`)
- rename bank facts to interactions (`b4a2915`)
- rename bank facts to interactions (`8e575ce`)
- rename bank facts to interactions (`377f551`)
- rename bank facts to interactions (`425c6f3`)
- speed up batch writes (`91bc3b0`)
- speed up batch writes (`3402bf1`)
- fix docker image (#14) (`bb434f3`)
- Release v0.0.17 (`70f09ef`)
- mcp test (`d72a339`)
- improve docker and mcp (`b83fd3a`)
- cli installation (`e0cfec1`)
- Release v0.0.16 (`27d00f3`)
- docs, packages and quick start (`6073ac4`)
- add repo files (`9b69202`)
- add repo files (`bb6bec5`)
- fix readme github images (`4b8fccb`)

### 2025-12-03

**Other:**
- Fix architecture link (#13) (`1c5981b`)
- Add license (#12) (`b0d71e2`)
- Release v0.0.15 (`e57e906`)
- .dockerignore (`38f3589`)
- fix docker cp image build on ci (#10) (`58592d4`)
- Release v0.0.14 (`a140247`)
- swith cp docker image to musl (`27a7d56`)
- Release v0.0.13 (`2c0e5d1`)
- fix release (`b0961cc`)
- fix paper (`9e1daa0`)
- Release v0.0.12 (`a4a9743`)
- fix release script (`902eed7`)
- Release v0.0.11 (`a8c23b1`)
- update npm package name (`850d403`)
- Release v0.0.10 (`b75c796`)
- Release v0.0.9 (`446fd84`)
- fix release (`1bf08c1`)
- Release v0.0.8 (`9522747`)
- prepare for release (`09fbcc2`)

**Fixed:**
- ci and ui build (#9) (`050a3d2`)
- ci and ui improvements (#8) (`e142435`)

### 2025-12-02

**Other:**
- control plane and api issues (`943f6e7`)

### 2025-12-01

**Other:**
- fix regressions and bunch of issues (`f7cf33c`)

### 2025-11-29

**Other:**
- fix python client (`e75c483`)

### 2025-11-28

**Other:**
- chunks (`3e72984`)
- fix compile (`18a7dc4`)

### 2025-11-27

**Other:**
- rename to new names (`d099ef8`)

### 2025-11-26

**Other:**
- many improvements (`a3ad76d`)

### 2025-11-25

**Other:**
- rename to hindsight (#2) (`b1ff2e8`)

### 2025-11-24

**Other:**
- Update Docusaurus configuration for new project (`156b151`)
- Create deploy-docs.yml (`c5b5681`)
- add docs and some fixes (`a2ac37e`)
- add docs and some fixes (`4f8e4b8`)

### 2025-11-23

**Other:**
- Fix broken dependency (`2bedc00`)

### 2025-11-22

**Other:**
- Fix docker issues and dependencies (`bffd69e`)
- Fix dependencies and docker (`9b36a3a`)

### 2025-11-21

**Other:**
- add cli config and mcp (`4ffef6c`)

### 2025-11-20

**Other:**
- merge mcp (`536da41`)

### 2025-11-19

**Other:**
- more (`99a54ae`)

### 2025-11-18

**Other:**
- openai and langmem integrations (`aa35223`)

### 2025-11-17

**Other:**
- t5 (`5ab7769`)
- t5 (`b894e81`)
- temporal (`b095a38`)
- temporal (`1b29635`)

### 2025-11-14

**Other:**
- temporal offset and others (`e15cbb4`)
- papers and fixes (`3e618cb`)

### 2025-11-13

**Other:**
- fix agnts (`f521b36`)
- fix startup (`3f22dfc`)
- fix startup (`8f25fc8`)
- fix startup (`dbac10a`)
- Release v0.0.7 (`dbdcf47`)
- refactori api (`28e28d5`)
- add profiles (`c82fff9`)

### 2025-11-11

**Other:**
- Release v0.0.5 (`9edf430`)
- fix space (`47d08de`)
- Release v0.0.4 (`434d869`)
- mcp server (`e0a33f5`)
- Release v0.0.3 (`fb45440`)
- update gh actions (`7d8e1fc`)
- Release v0.0.2 (`3e2a565`)
- update gh actions (`6024321`)
- Release v0.0.1 (`7960d9f`)
- update gh actions (`49d9b4b`)
- polish + cli + helm + standalone (`5880651`)

### 2025-11-10

**Other:**
- standalone and fix (`a1c5f98`)
- reorder files (`646dea8`)
- lot of improvements (`ada9562`)

### 2025-11-07

**Other:**
- improve moniitoring (`be8570d`)
- improve moniitoring (`cfafd2f`)
- add pending ops (`bff72ca`)
- fixes (`0f33c80`)
- fixes (`5d32e79`)
- fixes (`7a9f4ee`)
- fix iuo (`27bb335`)
- fix web init (`18c7e08`)

### 2025-11-06

**Other:**
- bm25 and re-rankers (`fcc5250`)

### 2025-11-05

**Other:**
- local setup and speed (`39801d9`)
- perf think improvements (`d8fdbb9`)
- perf think improvements (`ba048ff`)

### 2025-11-04

**Other:**
- cleanup (`45b3a68`)

### 2025-11-03

**Other:**
- emb abstraction (`1b7e0bc`)
- emb abstraction (`992faa4`)
- memora (`7d8d07d`)
- think feat (`42260c2`)

### 2025-10-31

**Other:**
- more fixes (`d8d48d6`)
- mrr and links reinforcement (`b3259d2`)
- improvements benchmark and perf (`75f40fc`)

### 2025-10-30

**Other:**
- improvements async (`8c698d6`)
- improvements (`bc7d9fe`)
- initial commit (`6d7223b`)
<!-- SQUEEGEE:AUTO:END changelog-entries -->

---

*Auto-generated by Squeegee Documentation System. Manual notes can be added outside the markers.*
